C coWW-faddrlx-faddrlx-lrlx
{ [x] = 0; }

P0 (int* x) {
  atomic_fetch_add_explicit(x, 1, memory_order_relaxed);
  atomic_fetch_add_explicit(x, 1, memory_order_relaxed);
}


P1 (int* x) {
  atomic_load_explicit(x, memory_order_relaxed);
}

~exists ([x]=0 \/ [x]=1)